schemaVersion: 3
service:
  name: legal
  archetype: java-maven-azure
tests:
  acceptance:
    type: maven
    path: legal-acceptance-test
    mavenArguments: [test]
    timeoutMinutes: 15
    bindings:
      HOST: { source: gateway }
      DATA_PARTITION_ID: { source: partition }
      PRIVILEGED_USER_TOKEN: { source: token }
      EXPOSE_FEATUREFLAG_ENABLED: { source: static, value: "true" }
